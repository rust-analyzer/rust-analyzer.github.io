= Changelog #330
:sectanchors:
:experimental:
:page-layout: post

Commit: commit:c5d30e2331acb2cec913a086ab242591f4f367a5[] +
Release: release:2026-06-01[] (`v0.3.2921`)

== New Features

* pr:22411[] add `cannot-implicitly-deref-trait-object` diagnostic.

== Performance Improvements

* pr:22493[] make `MemDocs` cheap to clone since it's snapshotted.

== Fixes

* pr:22473[] port block and loop inference from `rustc`.
* pr:22476[] only complete functional record update at the end of the literal.
* pr:22449[] fix an expected analysis regression with enum variants.
* pr:22409[], pr:22453[] filter out reference completions for generic reference parameters.
* pr:22452[] prioritize missing fields in pattern completion.
* pr:22437[] handle macros in `extract_module`.
* pr:22447[] fix `extract_variable` in macro calls.
* pr:22498[] also consider library features internal.
* pr:22426[] fix potential panic in `CfgDiff` formatting.
* pr:22342[] don't double references with `&mut T` in `add_reference_here`.
* pr:21566[] fix `SyntaxContext` roots potentially overlapping interneds.

== Internal Improvements

* pr:22465[] improve syntax mapping in `for` loop desugaring.
* pr:22474[] intern MIR projections using the new type interning infrastructure.
* pr:22477[], pr:22484[] replace `ast::make` with `SyntaxFactory` in `derive_macro`, `edits`, `prettify_macro_expansion_` and `rename`.
* pr:22500[] always use sysroot crates for `proc-macro-srv`.
* pr:22427[] fix inactive region handling in `MiniCore`.
* pr:22501[] don't depend on `salsa` for the `proc-macro-srv` CI job.
* pr:22492[], pr:22502[] use 1 CGU for release builds.
